#ifndef FALLOUT_PRESENTER_CLIENT_H_
#define FALLOUT_PRESENTER_CLIENT_H_

namespace fallout {

// Installs the presenter that reproduces legacy UI behavior (client side).
void presenterInstallClient();

} // namespace fallout

#endif /* FALLOUT_PRESENTER_CLIENT_H_ */
